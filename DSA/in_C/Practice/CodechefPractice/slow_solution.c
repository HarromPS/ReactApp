#include <stdio.h>

int main(void) {
	int T,count=0;
    scanf("%d",&T);
    int maxT,maxN,sumN;
    for(int i=0;i<T;i++)
    {
        // scanf("%d %d %d ",&maxT,&maxN,&sumN);
        // su
        for(int i=0;i<T;i++)
        {
            count++;
        }
    }
    printf("%d ",count);
    // for(int i=0;i<T;i++)
    // {
    //     // sumN+=
    // }
	return 0;
}

