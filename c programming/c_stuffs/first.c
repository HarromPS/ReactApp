#include <stdio.h>
int main() // int gives a output i.e. return 0
           // execution of program starts from main()

{
    int a = 1; // integer value stored in variable a
    // int b=2.2; is not recommended
    float c = 2.3; // is correct data type for decimal
                   // i.e real values
    printf("integer value is stored in a-%d\n", a);
    printf("Value of b is %f\n", c);
    // DIY --> create a program to add two numbers in C

    int x= 3, y=8;
    printf ("Sum of x & y is %d\n",x+y);
    
    
    return 0;
}