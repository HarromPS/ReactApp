#include <stdio.h>
void strcpy(char *source, char target)
{
   // for (int i=0;i<)
}

int main()
{
    // Q5 Write your own function of strcpy() from <string.h>

    char source[] = "HelloWorld";
    char target[30];
    strcpy(target,source);

    return 0;
}